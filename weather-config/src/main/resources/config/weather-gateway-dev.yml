zuul:
  routes:
    weather:
      path: /weather/**
      serviceId: weather-collect-amap-service
    city:
      path: /city/**
      serviceId: weather-city-amap-service
    web:
      path: /center/**
      serviceId: weather-web
  ratelimit:
    enabled: true
    repository: redis
    policy-list:
      weather:
        - limit: 100
          refresh-interval: 1
          type:
            - origin
      city:
        - limit: 100
          refresh-interval: 1
          type:
            - origin
    