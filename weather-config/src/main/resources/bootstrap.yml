spring:
  application:
    name: weather-config
  # 配置中心
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/
  profiles:
    active: native
server:
  port: 6000
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8000/eureka/