spring:
  application:
    name: weather-config
  # 配置中心
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/
    nacos:
      discovery:
        server-addr: 106.12.88.80:8848
  profiles:
    active: native
server:
  port: 6000